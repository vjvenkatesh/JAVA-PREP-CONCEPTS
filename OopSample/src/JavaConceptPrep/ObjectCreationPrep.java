package JavaConceptPrep;

public class ObjectCreationPrep {
    //Object creation ways
    //Sample sample=new Sample(); // Here we are initialising the object and calling the default
    // constructor while creating the Object There are two things happenening in background

    /*
    1.Memory Allocation - when object is created, for that object memory will allocated in the heap.
    2.Initialization - the constructor of the class is called .
    */

}
