package JavaConceptPrep;

public class ConstructorRole {
        /*
        This block will contain the constructor's
            int number;
            String name;

            //When we are creating object of an ConstructorRole class by default
             it will call the default constructor on this default const we are doing the constructor
             chaining concept to calling parameterized constructor.

            public ConstructorRole(){
                this(4,"tara");
            }
            public ConstructorRole(int number, String name){
                this.number=number;
                this.name = name;
            }

         */
    /*

     */
}
